"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Asset');
const Fixture = require('../../fixtures/AssetFixture');

describe('models:Asset', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
