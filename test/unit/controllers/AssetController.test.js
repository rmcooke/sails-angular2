"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/AssetController');

describe('controllers:AssetController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
